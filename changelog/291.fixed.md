- Fixed a bug where interrupting the bot during a `tts_say` or `function` action could leave the ongoing actions count permanently out of sync, dangling the task awaiting `execute_actions()` and preventing deferred `post_actions` from ever running again.
