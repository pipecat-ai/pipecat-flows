- Single-argument (legacy) action handlers are deprecated and will be removed in 2.0.0. Update handlers to accept `(action: dict, flow_manager: FlowManager)`. A `DeprecationWarning` is emitted once per `ActionManager` instance the first time a legacy action handler is invoked.
